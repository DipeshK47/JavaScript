"use strict"; // treat all JS code as newer version.

// Alert Statement
alert() //alert popup on browser.

// Number
let num = 42; // Integer
let float = 3.14; // Floating-point number

// String
let str = "Hello, world!";

// Boolean
let isTrue = true;
let isFalse = false;

// Undefined
let undefinedVar;

// Null
let nullVar = null;

// Symbol
let sym = Symbol('description');

// BigInt
let bigInt = 1234567890123456789012345678901234567890n;

// Object
let obj = { name: "John", age: 30 }; // Object
let arr = [1, 2, 3]; // Array, which is a type of object
let func = function() { return "Hello"; }; // Function, which is a type of object

console.log(num, float, str, isTrue, isFalse, undefinedVar, nullVar, sym, bigInt, obj, arr, func());